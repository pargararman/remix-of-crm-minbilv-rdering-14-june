import { describe, it, expect } from "vitest";

// Canonical Blocket provider tests live in test/blocket-provider.test.ts.
// This root-level file is kept only so older Lovable/test globs do not run stale logic.
describe("canonical Blocket provider tests", () => {
  it("are located in test/blocket-provider.test.ts", () => {
    expect(true).toBe(true);
  });
});
